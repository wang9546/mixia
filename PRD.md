# 米夏婚礼小程序产品需求文档（PRD）

---

## 一、产品概述

### 1.1 产品定位
米夏婚礼是一款专注于婚礼服务的综合展示平台，为新人提供婚纱摄影、现场布置、婚车租赁、四大金刚（摄影、摄像、化妆、主持）等婚礼相关服务的一站式展示体验。

### 1.2 产品目标
- **核心目标**：帮助新人便捷浏览婚礼服务，提升商家曝光与转化
- **商业目标**：打造本地婚礼服务行业的优质展示平台，建立品牌口碑
- **用户目标**：降低新人筹备婚礼的信息获取成本，提供真实案例参考

### 1.3 产品口号
「米夏婚礼，让爱有迹可循」

---

## 二、目标用户分析

### 2.1 主要用户画像

| 用户类型 | 特征描述 | 核心诉求 |
|---------|---------|---------|
| 准新人（23-35岁） | 处于备婚阶段，即将举办婚礼 | 寻找合适的婚礼服务商，查看真实案例，对比价格 |
| 新人父母 | 协助子女操办婚礼 | 传统观念强，注重性价比和口碑 |
| 婚礼策划师 | 职业需求，为客户推荐 | 需要丰富的案例库和供应商资源 |

### 2.2 用户场景
- **场景1**：周末刷小程序浏览婚纱摄影作品，收藏喜欢的风格
- **场景2**：查看婚车实拍照片，了解租车价格和车型信息
- **场景3**：为婚礼挑选摄影师，查看其过往作品集
- **场景4**：通过后台管理上下架商品，更新案例照片

---

## 三、信息架构

### 3.1 小程序端页面结构

```
首页
├── 快捷入口（四大模块图标：婚纱摄影/现场布置/婚车租赁/四大金刚）
├── 推荐展示区（精选案例）
└── 底部导航栏（首页/我的）

婚纱摄影模块（子分类由后台配置）
├── 分类列表（城市/风格，后台可配置）
├── 作品列表页（分页加载）
└── 作品详情页

现场布置模块（子分类由后台配置）
├── 分类列表（舞台/背景墙/花艺/灯光，后台可配置）
├── 案例列表页
└── 案例详情页

婚车租赁模块（子分类由后台配置）
├── 品牌分类（后台可配置）
├── 车辆列表页（分页加载）
└── 车辆详情页

四大金刚模块（子分类由后台配置）
├── 类型分类（摄影/摄像/化妆/主持，后台可配置）
├── 金刚列表页（分页加载）
└── 金刚详情页（含作品案例）

我的
├── 我的收藏
├── 浏览历史
└── 联系客服（门店信息展示+一键拨号+导航）

```

### 3.2 后台管理系统结构

```
运营后台
├── 仪表盘（数据概览）
├── 内容管理
│   ├── 婚纱摄影管理（CRUD）
│   ├── 现场布置管理（CRUD）
│   ├── 婚车管理（CRUD）
│   ├── 四大金刚管理（CRUD）
│   └── 分类配置管理
├── 案例作品管理
│   ├── 作品管理（图片/视频）
│   └── 案例管理
├── 门店信息管理
├── 用户管理
├── 系统设置
└── 数据统计
```

---

## 四、功能详细说明

### 4.1 婚纱摄影展示模块

#### 4.1.1 功能描述
展示婚纱摄影作品，支持图片和视频展示，分类筛选，分页浏览。

#### 4.1.2 用户流程
```
进入模块 → 选择分类（城市/风格） → 浏览作品列表（分页加载）
→ 点击查看详情 → 收藏/联系客服
```

#### 4.1.3 功能清单

| 功能点 | 说明 | 优先级 |
|-------|------|-------|
| 分类筛选 | 支持按一级/二级标签筛选 | P0 |
| 作品列表 | 瀑布流布局，图片+视频标识 | P0 |
| 分页加载 | 下拉加载更多，20条/页 | P0 |
| 作品详情 | 图片/视频轮播+作品描述 | P0 |
| 视频播放 | 支持多视频列表播放，全屏支持 | P0 |


#### 4.1.4 页面设计要求
- **列表页**：瀑布流布局，图片比例3:4，标识视频封面
- **详情页**：
  - 顶部图片/视频轮播（视频自动播放首帧封面）
  - 作品标题、拍摄地点、风格标签
  - 作品描述
  - 联系客服按钮（固定底部）

---

### 4.2 现场布置展示模块

#### 4.2.1 功能描述
展示婚礼现场布置案例，包含舞台、背景墙、花艺、灯光等类型。

#### 4.2.2 用户流程
```
进入模块 → 选择分类（舞台/背景墙/花艺/灯光） → 浏览案例列表
→ 点击查看案例详情 → 联系客服
```

#### 4.2.3 功能清单

| 功能点 | 说明 | 优先级 |
|-------|------|-------|
| 分类筛选 | 支持按类型筛选 | P0 |
| 案例列表 | 瀑布流/网格展示，多图预览 | P0 |
| 案例详情 | 多图轮播+场景描述+服务商信息 | P0 |
| 作品图册 | 支持多图展示，营造沉浸感 | P0 |
| 收藏功能 | 收藏案例至"我的收藏" | P1 |

#### 4.2.4 页面设计要求
- **列表页**：瀑布流布局，图片高度自适应，展示真实布置效果
- **详情页**：
  - 多图轮播展示（可左右滑动）
  - 案例名称、风格标签
  - 场景描述（适用酒店、婚礼风格等）
  - 关联服务商信息
  - 联系客服入口

---

### 4.3 婚车租赁展示模块

#### 4.3.1 功能描述
展示婚车车型、价格，支持按品牌筛选，分页浏览。

#### 4.3.2 用户流程
```
进入模块 → 选择品牌/车型 → 浏览车辆列表（分页加载）
→ 查看车辆详情 → 联系客服
```

#### 4.3.3 功能清单

| 功能点 | 说明 | 优先级 |
|-------|------|-------|
| 品牌筛选 | 按婚车品牌标签筛选 | P0 |
| 车型列表 | 卡片展示：车型图片+名称+日租金 | P0 |
| 分页加载 | 下拉加载更多，20条/页 | P0 |
| 车辆详情 | 多图展示+配置信息+日租金 | P0 |
| 价格说明 | 明确日租金、超时费用、公里限制 | P0 |
| 车型对比 | 支持添加对比栏，对比2-3款车型 | P2 |

#### 4.3.4 页面设计要求
- **列表页**：横向滚动品牌筛选栏 + 车辆卡片网格，分页加载
- **详情页**：
  - 车辆图片/视频轮播
  - 车型名称、品牌
  - 座位数、颜色、排量
  - 日租金（醒目展示）
  - 联系客服按钮（固定底部）

---

### 4.4 四大金刚展示模块

#### 4.4.1 功能描述
展示摄影师、摄像师、化妆师、主持人，支持按类型筛选和查看作品案例，分页浏览。

#### 4.4.2 用户流程
```
进入模块 → 选择类型（摄影/摄像/化妆/主持） → 浏览金刚列表（分页加载）
→ 查看详情/作品集 → 联系客服
```

#### 4.4.3 功能清单

| 功能点 | 说明 | 优先级 |
|-------|------|-------|
| 类型筛选 | 按摄影/摄像/化妆/主持标签筛选 | P0 |
| 金刚列表 | 头像+姓名+简介+价格区间 | P0 |
| 分页加载 | 下拉加载更多，20条/页 | P0 |
| 金刚详情 | 个人介绍+服务内容+价格 | P0 |
| 作品案例 | 作品图册/视频展示（关联作品管理） | P0 |
| 视频播放 | 支持作品视频播放 | P0 |
| 评价展示 | 用户评价列表 | P1 |

#### 4.4.4 特殊说明：作品案例关联
- 每个金刚可关联多个作品案例
- 作品案例包含：作品名称、类型（图片/视频）、展示图片/封面、视频URL
- 作品详情页可点击返回金刚主页

#### 4.4.5 页面设计要求
- **列表页**：横向类型Tab + 金刚卡片列表，分页加载
- **详情页**：
  - 金刚头像、姓名、从业年限
  - 服务内容描述
  - 价格区间
  - 作品案例展示（图片网格，点击查看大图）
  - 联系客服按钮

---

### 4.5 联系客服模块

#### 4.5.1 功能描述
展示门店信息，支持一键拨号和导航功能。

#### 4.5.2 功能清单

| 功能点 | 说明 | 优先级 |
|-------|------|-------|
| 门店信息展示 | 门店名称、地址、电话、营业时间、简介、图片 | P0 |
| 一键拨号 | 点击电话直接拨打 | P0 |
| 导航功能 | 调用微信地图定位并导航 | P0 |
| 门店信息后台配置 | 运营人员在后台管理门店信息 | P0 |

#### 4.5.3 页面设计要求
- **页面内容**：
  - 门店图片轮播
  - 门店名称、简介
  - 地址信息（可点击跳转地图）
  - 联系电话（醒目展示，支持点击拨打）
  - 营业时间
  - 一键拨号按钮（固定底部）
  - 导航按钮（跳转微信地图）

---

### 4.6 后台管理系统模块

#### 4.6.1 功能描述
为运营人员提供完整的内容管理、用户管理、数据统计等功能。

#### 4.6.2 功能清单

| 模块 | 功能点 | 说明 | 优先级 |
|-----|-------|------|-------|
| 仪表盘 | 数据概览 | 总商品数、总访问量 | P0 |
| 仪表盘 | 数据趋势 | 访问量趋势图 | P0 |
| 婚纱礼服管理 | 商品CRUD | 添加/编辑/删除/上下架商品 | P0 |
| 婚纱礼服管理 | 批量操作 | 批量上下架、批量删除 | P1 |
| 现场布置管理 | 案例CRUD | 添加/编辑/删除/上下架案例 | P0 |
| 婚车管理 | 车辆CRUD | 添加/编辑/删除/上下架车辆 | P0 |
| 四大金刚管理 | 金刚CRUD | 添加/编辑/删除/上下架金刚 | P0 |
| 四大金刚管理 | 作品管理 | 管理金刚关联的作品案例 | P0 |
| 标签管理 | 标签CRUD | 添加/编辑/删除/启用禁用标签 | P0 |
| 标签管理 | 标签分组 | 一级标签、二级标签管理 | P0 |
| 门店信息管理 | 门店CRUD | 编辑门店名称、地址、电话、营业时间等 | P0 |
| 用户管理 | 用户列表 | 查看注册用户 | P1 |
| 数据统计 | 访问统计 | 页面访问量趋势 | P2 |

#### 4.6.3 数据统计指标
- 总访问量/日访问量
- 各模块浏览量


---

## 五、数据字段设计

### 5.1 婚纱摄影作品表（mixia_dress）

| 字段名 | 类型 | 说明 | 必填 |
|-------|------|------|------|
| _id | ObjectId | 主键 | 是 |
| name | String | 作品名称 | 是 |
| cover_image | String | 封面图URL | 是 |
| images | Array | 图片URL列表 | 是 |
| videos | Array | 视频列表（见下方） | 否 |
| price | Number | 价格（分为单位，0表示面议） | 是 |
| description | String | 作品描述 | 否 |
| level1_tags | Array | 一级标签ID列表（如城市等） | 是 |
| level2_tags | Array | 二级标签ID列表（如风格等） | 是 |
| is_active | Boolean | 是否上架 | 是 |
| sort_order | Number | 排序值 | 否 |
| view_count | Number | 浏览次数 | 否 |
| favorite_count | Number | 收藏次数 | 否 |
| create_time | Date | 创建时间 | 是 |
| update_time | Date | 更新时间 | 是 |

**视频字段（videos[]）结构：**
| 字段名 | 类型 | 说明 |
|-------|------|------|
| url | String | 视频URL |
| cover_url | String | 视频封面URL |
| duration | Number | 时长（秒） |
| size | Number | 文件大小（字节） |
| width | Number | 宽度 |
| height | Number | 高度 |

### 5.2 现场布置表（mixia_decoration）

| 字段名 | 类型 | 说明 | 必填 |
|-------|------|------|------|
| _id | ObjectId | 主键 | 是 |
| title | String | 案例标题 | 是 |
| cover_image | String | 封面图URL | 是 |
| images | Array | 图片URL列表 | 是 |
| videos | Array | 视频列表（见下方结构） | 否 |
| style | String | 风格：中式/西式/森系/韩式 | 否 |
| venue | String | 适用场地 | 否 |
| description | String | 案例描述 | 否 |
| vendor_name | String | 供应商名称 | 否 |
| level1_tags | Array | 一级标签ID列表（如户外、室内等） | 是 |
| level2_tags | Array | 二级标签ID列表（如婚庆、订婚宴等） | 是 |
| is_active | Boolean | 是否上架 | 是 |
| sort_order | Number | 排序值 | 否 |
| view_count | Number | 浏览次数 | 否 |
| create_time | Date | 创建时间 | 是 |
| update_time | Date | 更新时间 | 是 |

### 5.3 婚车表（mixia_car）

| 字段名 | 类型 | 说明 | 必填 |
|-------|------|------|------|
| _id | ObjectId | 主键 | 是 |
| model | String | 车型 | 是 |
| images | Array | 图片URL列表 | 是 |
| videos | Array | 视频列表（见下方结构） | 否 |
| brand_tag_id | ObjectId | 品牌标签ID | 是 |
| color_tag_id | ObjectId | 颜色标签ID | 是 |
| seats | Number | 座位数 | 否 |
| displacement | String | 排量 | 否 |
| daily_price | Number | 日租金（分为单位） | 是 |
| description | String | 车辆描述 | 否 |
| is_active | Boolean | 是否上架 | 是 |
| sort_order | Number | 排序值 | 否 |
| view_count | Number | 浏览次数 | 否 |
| create_time | Date | 创建时间 | 是 |
| update_time | Date | 更新时间 | 是 |

### 5.4 四大金刚表（mixia_vendor）

| 字段名 | 类型 | 说明 | 必填 |
|-------|------|------|------|
| _id | ObjectId | 主键 | 是 |
| name | String | 金刚姓名 | 是 |
| type_tag_id | ObjectId | 类型标签ID（摄影/摄像/化妆/主持） | 是 |
| avatar | String | 头像URL | 是 |
| phone | String | 联系电话 | 否 |
| years_exp | Number | 从业年限 | 否 |
| price_range | String | 价格区间，如"3000-5000" | 否 |
| service_desc | String | 服务内容描述 | 否 |
| work_samples | Array | 作品案例ID列表（关联works表） | 否 |
| is_active | Boolean | 是否上架 | 是 |
| sort_order | Number | 排序值 | 否 |
| view_count | Number | 浏览次数 | 否 |
| create_time | Date | 创建时间 | 是 |
| update_time | Date | 更新时间 | 是 |

### 5.5 作品案例表（mixia_work）

| 字段名 | 类型 | 说明 | 必填 |
|-------|------|------|------|
| _id | ObjectId | 主键 | 是 |
| title | String | 作品标题 | 是 |
| vendor_id | ObjectId | 所属金刚ID | 是 |
| type | String | 类型：图片/视频 | 是 |
| cover_image | String | 封面图URL | 是 |
| images | Array | 图片URL列表（type为图片时） | 否 |
| videos | Array | 视频列表（见下方结构） | 否 |
| description | String | 作品描述 | 否 |
| view_count | Number | 浏览次数 | 否 |
| is_active | Boolean | 是否展示 | 是 |
| create_time | Date | 创建时间 | 是 |

**视频字段（videos[]）结构：**
| 字段名 | 类型 | 说明 |
|-------|------|------|
| url | String | 视频URL |
| cover_url | String | 视频封面URL |
| duration | Number | 时长（秒） |
| size | Number | 文件大小（字节） |
| width | Number | 宽度 |
| height | Number | 高度 |

### 5.6 标签表（mixia_tag）

| 字段名 | 类型 | 说明 | 必填 |
|-------|------|------|------|
| _id | ObjectId | 主键 | 是 |
| module | String | 所属模块：dress/decoration/car/vendor | 是 |
| name | String | 标签名称 | 是 |
| level | Number | 层级：1（一级）/2（二级） | 是 |
| parent_id | ObjectId | 父级标签ID（一级标签时为null） | 否 |
| sort_order | Number | 排序值 | 否 |
| is_active | Boolean | 是否启用 | 是 |
| create_time | Date | 创建时间 | 是 |
| update_time | Date | 更新时间 | 是 |

### 5.7 用户表（mixia_user）

| 字段名 | 类型 | 说明 | 必填 |
|-------|------|------|------|
| _id | ObjectId | 主键 | 是 |
| openid | String | 微信openid | 是 |
| nickName | String | 微信昵称 | 否 |
| avatarUrl | String | 头像URL | 否 |
| gender | String | 性别 | 否 |
| phone | String | 手机号 | 否 |
| create_time | Date | 注册时间 | 是 |
| update_time | Date | 更新时间 | 是 |

### 5.8 门店信息表（mixia_store）

| 字段名 | 类型 | 说明 | 必填 |
|-------|------|------|------|
| _id | ObjectId | 主键 | 是 |
| name | String | 门店名称 | 是 |
| address | String | 门店地址 | 是 |
| phone | String | 联系电话 | 是 |
| longitude | Number | 经度 | 否 |
| latitude | Number | 纬度 | 否 |
| business_hours | String | 营业时间 | 否 |
| description | String | 门店简介 | 否 |
| images | Array | 门店图片列表 | 否 |
| is_active | Boolean | 是否启用 | 是 |
| update_time | Date | 更新时间 | 是 |

### 5.9 用户收藏表（mixia_favorite）

| 字段名 | 类型 | 说明 | 必填 |
|-------|------|------|------|
| _id | ObjectId | 主键 | 是 |
| user_openid | String | 用户openid | 是 |
| item_type | String | 收藏类型：dress/car/vendor/decoration | 是 |
| item_id | ObjectId | 收藏项目ID | 是 |
| create_time | Date | 收藏时间 | 是 |

---

## 六、缓存机制设计

### 6.1 设计目的
减少云函数调用次数，降低开发调试成本，提升页面加载速度。

### 6.2 缓存策略

| 数据类型 | 缓存位置 | 缓存时长 | 更新策略 |
|---------|---------|---------|---------|
| 模块标签（婚纱礼服） | 小程序本地Storage | 24小时 | 后台修改标签时清除 |
| 模块标签（现场布置） | 小程序本地Storage | 24小时 | 后台修改标签时清除 |
| 模块标签（婚车） | 小程序本地Storage | 24小时 | 后台修改标签时清除 |
| 模块标签（四大金刚） | 小程序本地Storage | 24小时 | 后台修改标签时清除 |
| 商品/案例列表 | 小程序本地Storage | 30分钟 | 后台上架/下架时清除 |
| 门店信息 | 小程序本地Storage | 24小时 | 后台修改时清除 |
| 用户收藏列表 | 不缓存 | - | 实时从云数据库获取 |

### 6.3 缓存实现方案

#### 6.3.1 本地缓存封装
```javascript
// 缓存工具类
class CacheManager {
  // 设置缓存
  static set(key, data, expireTime) {
    wx.setStorageSync(key, {
      data: data,
      expire: Date.now() + expireTime
    });
  }

  // 获取缓存
  static get(key) {
    const cache = wx.getStorageSync(key);
    if (!cache) return null;
    if (Date.now() > cache.expire) {
      wx.removeStorageSync(key);
      return null;
    }
    return cache.data;
  }

  // 清除指定缓存
  static remove(key) {
    wx.removeStorageSync(key);
  }

  // 清除所有缓存
  static clear() {
    wx.clearStorageSync();
  }
}
```

#### 6.3.2 数据获取流程
```
请求数据 →
  检查本地缓存是否存在且有效 →
    是 → 返回本地缓存数据
    否 → 调用云函数获取数据 → 写入本地缓存 → 返回数据
```

#### 6.3.3 标签数据加载流程
```
进入模块列表页 →
  检查本地标签缓存是否存在且有效 →
    是 → 直接使用缓存标签进行筛选
    否 → 调用云函数获取该模块的所有标签 →
      按一级/二级分组 →
      写入本地缓存 →
      展示筛选UI
```

#### 6.3.4 标签数据结构示例
```javascript
// 云函数返回的标签数据结构
{
  module: "dress",
  level1: [
    { _id: "tag001", name: "三亚", level: 1 },
    { _id: "tag002", name: "厦门", level: 1 },
    { _id: "tag003", name: "大理", level: 1 }
  ],
  level2: [
    { _id: "tag101", name: "旅拍", level: 2, parent_id: "tag001" },
    { _id: "tag102", name: "室内", level: 2, parent_id: "tag001" },
    { _id: "tag201", name: "海景", level: 2, parent_id: "tag002" }
  ]
}
```

#### 6.3.5 后台数据变更通知
- 后台修改数据后，调用云函数更新缓存版本号或直接清除相关缓存
- 小程序下次启动或下拉刷新时自动获取最新数据
- 版本号冲突时优先使用最新数据

### 6.5 缓存清除机制

| 触发场景 | 清除范围 |
|---------|---------|
| 后台发布新商品 | 对应模块的列表缓存 |
| 后台上架/下架 | 对应商品/案例的详情缓存 |
| 后台修改标签 | 对应模块的标签缓存 |
| 后台修改门店信息 | 门店信息缓存 |
| 用户登出 | 用户相关缓存（收藏等） |

---

## 七、视频处理与缓存设计

### 7.1 视频上传处理流程
```
用户选择视频 →
  前端压缩处理（分辨率压缩、码率降低） →
  上传到云存储 →
  生成视频封面 →
  获取视频元信息（时长、尺寸） →
  保存到云数据库
```

### 7.2 视频压缩配置
| 参数 | 说明 | 推荐值 |
|-----|------|-------|
| 分辨率 | 最大宽度 | 720p (1280px) |
| 码率 | 视频码率 | 1500kbps |
| 帧率 | 视频帧率 | 25fps |
| 封面 | 自动截取第1秒 |

### 7.3 视频缓存策略
| 缓存类型 | 缓存位置 | 时长 | 说明 |
|---------|---------|------|------|
| 视频封面 | 本地Storage | 24小时 | 减少图片请求 |
| 视频URL | 不缓存 | - | 直接使用云存储URL |
| 视频播放进度 | 本地Storage | 会话内 | 支持断点续播 |

### 7.4 列表页分页加载
| 参数 | 说明 |
|-----|------|
| 每页数量 | 20条 |
| 加载方式 | 下拉触底加载 |
| 排序规则 | 按sort_order降序，默认 |
| 筛选条件 | 标签筛选（支持多选） |

---

## 八、运营管理需求

### 8.1 内容运营
- 定期上传新的婚纱摄影作品和布置案例
- 金刚作品案例定期更新，展示最新作品

### 8.2 数据监控
- 监控各模块浏览量，优化高流量模块


---

## 九、UI设计规范（高级感）

### 9.1 设计原则
- **简约克制**：留白充足，内容聚焦，避免信息过载
- **精致细节**：圆角、阴影、渐变恰到好处
- **统一节奏**：一致的间距、字号、色彩体系

### 9.2 字体规范
| 用途 | 字号 | 字重 | 说明 |
|-----|------|------|------|
| 标题 | 18-20px | 600 | 模块标题、详情页标题 |
| 正文 | 14-15px | 400 | 商品描述、列表文案 |
| 辅助 | 12-13px | 400 | 时间、标签、说明文字 |
| 价格 | 16-18px | 600 | 价格数字突出显示 |

### 9.3 间距规范
- 页面边距：32rpx（两侧）
- 卡片间距：24rpx
- 卡片内边距：24rpx
- 标签间距：16rpx
- 底部安全区：避免内容被导航遮挡

### 9.4 卡片设计
- 圆角：16rpx（统一）
- 阴影：`0 4rpx 20rpx rgba(0,0,0,0.06)`
- 封面图：3:4比例（婚纱摄影）、1:1（婚车/金刚头像）
- 视频标识：右下角播放图标 + 时长

### 9.5 动效设计
- 页面切换：淡入淡出，300ms
- 列表加载：淡入 + 微上移，200ms间隔
- 按钮点击：scale(0.98)，100ms
- 骨架屏：渐变动画，避免白屏
- 视频播放：点击后展开，全屏支持

### 9.6 列表页设计
- 瀑布流布局（婚纱摄影、现场布置）
- 网格布局（婚车：2列、金刚：横向卡片）
- 标签筛选：横向滚动Tab + 多选
- 分页加载：触底加载，显示loading动画

### 9.7 详情页设计
- 顶部轮播：全宽，支持视频
- 渐变遮罩：底部渐变遮罩提升文字可读性
- 悬浮按钮：联系客服按钮固定底部
- 图片查看：支持双指缩放

---

## 十、非功能性需求

### 10.1 性能要求
- 页面首屏加载时间 < 2秒
- 图片懒加载，减少流量消耗
- 视频首帧加载 < 1秒

### 10.2 兼容性要求
- 支持微信小程序基础库 v2.20+
- 适配不同手机屏幕尺寸
- 兼容iOS和Android系统

### 10.3 安全要求
- 用户手机号脱敏展示
- 云开发环境需正确配置安全规则
- 敏感操作需二次确认

### 10.4 微信特性适配
- 支持微信分享（生成小程序码海报）
- 支持微信支付
- 支持收藏至微信收藏夹

---

## 十一、项目规划

### 11.1 MVP版本（第一期）
- 首页 + 四大模块基础浏览
- 婚纱摄影/婚车/金刚详情页
- 联系客服功能
- 云数据库基础配置

### 11.2 迭代版本（第二期）
- 用户收藏功能
- 金刚作品案例关联展示
- 数据统计功能

### 11.3 优化版本（第三期）
- 用户评价功能
- 微信分享海报
- 会员积分体系

---

## 十二、风险评估

| 风险项 | 影响程度 | 应对策略 |
|-------|---------|---------|
| 图片加载慢 | 中 | 使用CDN加速，图片压缩，懒加载 |
| 视频加载卡顿 | 中 | 视频压缩、CDN加速、封面优先 |
| 商品数据量大 | 低 | 分页加载，搜索优化 |
| 竞品模仿 | 中 | 持续优化用户体验，建立品牌壁垒 |

---

## 文档信息

| 项目 | 内容 |
|-----|------|
| 产品名称 | 米夏婚礼 |
| 文档版本 | v1.0 |
| 创建日期 | 2026-04-06 |
| 文档状态 | 初稿 |
