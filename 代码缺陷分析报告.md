# 米夏婚礼小程序 — 深度优化分析报告

> 分析日期：2026-04-16（全量重新扫描）
> 分析范围：miniprogram/ + cloudfunctions/（当前全部文件）

---

## 目录

1. [高优先级（P1）](#一高优先级p1)
2. [中优先级（P2）](#二中优先级p2)
3. [低优先级 / 架构优化（P3）](#三低优先级--架构优化p3)
4. [优先级汇总](#四优先级汇总)

---

## 一、高优先级（P1）

### 1.1 `onPullDownRefresh` 调用 `loadData().then()` 可能崩溃

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `miniprogram/pages/list/index.js:46-49` |
| **严重程度** | ★★★★☆ 高 |

**问题描述**

`loadData` 在 `loading === true` 时直接 `return`（无返回值，即返回 `undefined`），而 `onPullDownRefresh` 对其结果调用 `.then()`：

```js
// onPullDownRefresh setData 回调中：
this.loadData(type).then(() => {   // loadData 返回 undefined → 直接崩溃
  wx.stopPullDownRefresh();
});
```

用户在列表加载过程中下拉刷新即可必现此问题，导致 `wx.stopPullDownRefresh()` 永远不执行，下拉动画持续转圈。

**改进方案**

在 `onPullDownRefresh` 的 `setData` 中同步重置 `loading: false`，确保 `loadData` 一定有机会执行：

```js
onPullDownRefresh: function () {
  const { type } = this.data;
  this.setData({
    list: [], leftList: [], rightList: [],
    leftHeight: 0, rightHeight: 0,
    page: 1, hasMore: true, loading: false   // ← 加这个
  }, () => {
    this.loadData(type).then(() => {
      wx.stopPullDownRefresh();
    });
  });
},
```

---

### 1.2 移除 `api.js` Toast 后，列表页加载失败无任何用户反馈

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `miniprogram/pages/list/index.js:95-97`、`admin/manage/index.js:80-84` |
| **严重程度** | ★★★★☆ 高 |

**问题描述**

`api.js` 的 Toast 已被移除（正确），但 `list/index.js` 和 `admin/manage/index.js` 的 `loadData` catch 块只剩 `console.error`，不向用户展示任何信息：

```js
} catch (err) {
  console.error('loadData err', err);  // 用户看不到任何提示
} finally {
  this.setData({ loading: false });
}
```

网络失败时页面仅呈现空白列表，用户无法区分"确实没有数据"还是"加载出错"。

**改进方案**

增加 `loadError` 状态并在 WXML 展示：

```js
// list/index.js data
data: { ..., loadError: false },

// catch 中
} catch (err) {
  console.error('loadData err', err);
  this.setData({ loadError: true });
  wx.showToast({ title: '加载失败，请下拉刷新', icon: 'none' });
}
```

---

### 1.3 `adminTag` 列表查询无 `.limit()` 限制，超 20 条标签时截断

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `cloudfunctions/adminTag/index.js:25-29` |
| **严重程度** | ★★★★☆ 高 |

**问题描述**

微信云数据库单次 `.get()` 不设 limit 时默认最多返回 **20** 条数据：

```js
const res = await db.collection('mixia_tag')
  .where(conditions)
  .orderBy('sort_order', 'asc')
  .get()   // 默认 limit 20，超出部分静默截断
```

当某模块标签超过 20 个时，admin 编辑页的标签选择框会缺少后面的标签，导致无法为内容打某些标签。`getTags`（面向用户）已正确使用 `.limit(100)`，唯独 `adminTag` 遗漏。

**改进方案**

```js
const res = await db.collection('mixia_tag')
  .where(conditions)
  .orderBy('sort_order', 'asc')
  .limit(200)   // 标签数量合理上限
  .get()
```

---

### 1.4 `compress.js` 并行上传使用 `Promise.all`，单张失败导致其他已上传文件孤立

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `miniprogram/utils/compress.js:116-127` |
| **严重程度** | ★★★☆☆ 高 |

**问题描述**

```js
const results = await Promise.all(
  chooseRes.tempFiles.map(file => compressAndUpload(...))
);
```

`Promise.all` 在任一 Promise reject 时立即 reject，其余已完成上传的文件的 `fileID` 全部丢失。这些文件已占用云存储空间，但因 `fileID` 未记录，永远无法被清除（云存储垃圾文件）。

**改进方案**

改用 `Promise.allSettled`，只收集成功项，并提示部分失败：

```js
const settledResults = await Promise.allSettled(
  chooseRes.tempFiles.map(file => {
    const cloudPath = 'images/' + Date.now() + '-' + Math.random().toString(36).substr(2) + '.jpg';
    return compressAndUpload(file.tempFilePath, cloudPath, { isVideo: false, imageQuality: quality, showProgress: false });
  })
);

const results = settledResults
  .filter(r => r.status === 'fulfilled')
  .map(r => r.value);

const failCount = settledResults.length - results.length;
if (failCount > 0) {
  wx.showToast({ title: `${failCount} 张上传失败`, icon: 'none' });
}
return results;
```

---

### 1.5 `admin/store/index.js` 的 `onSubmit` 中 `wx.hideLoading()` 不在 `finally`

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `miniprogram/pages/admin/store/index.js:171,174` |
| **严重程度** | ★★★☆☆ 高 |

**问题描述**

与已修复的 `admin/edit/index.js` 完全相同的问题：

```js
try {
  ...
  wx.hideLoading();     // try 内
  wx.showToast(...)
} catch (err) {
  wx.hideLoading();     // catch 内
  ...
}
```

若 `wx.showToast` 或 `setTimeout` 之后有异常，loading 遮罩将永久停留。

**改进方案**

```js
try { ... } catch (err) { ... } finally { wx.hideLoading(); }
```

---

## 二、中优先级（P2）

### 2.1 `getModuleList` 的 `count()` 和 `get()` 串行执行，可改为并行

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `cloudfunctions/getModuleList/index.js:29-36` |
| **严重程度** | ★★★☆☆ 中 |

**问题描述**

```js
const totalRes = await db.collection(collectionName).where(query).count()  // 等待
const listRes  = await db.collection(collectionName).where(query)...get()  // 再执行
```

两个查询完全独立，串行执行使列表接口耗时 ≈ 2× 单次查询时间。同样问题也存在于 `adminContent/index.js` 的 `list` action。

**改进方案**

```js
const [countRes, listRes] = await Promise.all([
  db.collection(collectionName).where(query).count(),
  db.collection(collectionName).where(query).field({...}).orderBy(...).skip(...).limit(...).get()
])
```

---

### 2.2 `getHomeData` 返回全量字段，包含 `images[]` 数组

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `cloudfunctions/getHomeData/index.js:12-29` |
| **严重程度** | ★★★☆☆ 中 |

**问题描述**

`getHomeData` 返回精选推荐列表，首页 WXML 只使用 `cover_image`/`avatar`、`name`/`title`/`model`、价格字段、`moduleType` 和 `_id`，但查询未限制字段，每条记录都带上 `images[]` 等大字段。与已修复的 `getModuleList` 同类问题。

**改进方案**

```js
const dressPromise = db.collection('mixia_dress')
  .where({ is_active: true, is_featured: true })
  .field({ _id: true, cover_image: true, name: true, price: true, is_featured: true })
  .orderBy('create_time', 'desc')
  .limit(2)
  .get()
// decorationPromise 同样处理
```

---

### 2.3 列表缓存 key 未对 tags 排序，顺序不同导致缓存穿透

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `miniprogram/utils/api.js:54` |
| **严重程度** | ★★★☆☆ 中 |

**问题描述**

```js
const cacheKey = `list_${moduleType}_p${page}_${tags.join('-')}`;
```

`['tagA', 'tagB']` 与 `['tagB', 'tagA']` 代表相同请求，但会生成不同 cache key，导致缓存失效、重复请求。虽然当前 UI 只能单选标签，但仍是隐患。

**改进方案**

```js
const cacheKey = `list_${moduleType}_p${page}_${[...tags].sort().join('-')}`;
```

---

### 2.4 `cache.js` 存取操作无错误捕获，存储异常会向上崩溃

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `miniprogram/utils/cache.js:5-20` |
| **严重程度** | ★★★☆☆ 中 |

**问题描述**

微信存储空间有 10MB 上限，或用户清除缓存后读取旧 key，均可能导致 `wx.setStorageSync` / `wx.getStorageSync` 抛出异常。当前没有 try-catch：

```js
static get(key) {
  const cache = wx.getStorageSync(key);  // 可能抛出
  ...
}
```

任何调用 `CacheManager.get()` 的代码都会因此崩溃。

**改进方案**

```js
static get(key) {
  try {
    const cache = wx.getStorageSync(key);
    if (!cache) return null;
    if (Date.now() > cache.expire) {
      wx.removeStorageSync(key);
      return null;
    }
    return cache.data;
  } catch (e) {
    return null;
  }
}

static set(key, data, expireTime = 24 * 60 * 60 * 1000) {
  try {
    wx.setStorageSync(key, { data, expire: Date.now() + expireTime });
  } catch (e) {
    console.error('cache set failed:', key, e);
  }
}
```

---

### 2.5 `adminContent` 的 `save` action 未做字段白名单，可写入任意字段

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `cloudfunctions/adminContent/index.js:92-101` |
| **严重程度** | ★★★☆☆ 中 |

**问题描述**

```js
case 'save': {
  const saveData = { ...itemData, update_time: db.serverDate() }
  // itemData 直接展开，客户端可传入任意字段覆盖 DB 记录
```

虽然 `save` 需要管理员权限，恶意管理员仍可传入 `{ _id: 'xxx', view_count: 999999 }` 等字段覆盖内部数据。

**改进方案**

对 `itemData` 做字段白名单过滤：

```js
const ALLOWED_FIELDS = ['name', 'title', 'model', 'description', 'service_desc',
  'cover_image', 'avatar', 'images', 'price', 'daily_price', 'price_range',
  'level1_tags', 'level2_tags', 'is_active', 'is_featured', 'sort_order',
  'seats', 'color_name', 'displacement', 'brand_name', 'years_exp', 'style', 'venue'];

const saveData = {};
ALLOWED_FIELDS.forEach(f => { if (itemData[f] !== undefined) saveData[f] = itemData[f]; });
saveData.update_time = db.serverDate();
```

---

### 2.6 `adminTag` 状态切换后触发完整列表重新加载，有多余云函数调用

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `miniprogram/pages/admin/tags/index.js:55-57` |
| **严重程度** | ★★☆☆☆ 中 |

**问题描述**

```js
toggleStatus: async function (e) {
  await API.adminTag('toggleStatus', { ... });
  this.loadTags();  // 触发额外的云函数调用刷新整个列表
}
```

操作成功后只需翻转本地 `is_active` 状态，无需重新请求全量数据：

**改进方案**

```js
const tags = this.data.tags.map(t =>
  t._id === id ? { ...t, is_active: !active } : t
);
this.setData({ tags });
```

---

### 2.7 `getModuleDetail` 直接修改 DB 返回对象属性，有副作用风险

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `cloudfunctions/getModuleDetail/index.js:51-62` |
| **严重程度** | ★★☆☆☆ 中 |

**问题描述**

```js
const detailData = res.data
// 直接修改原对象的 level1_tags 和 level2_tags
detailData.level1_tags = detailData.level1_tags.map(tagId => tagMap[tagId] || tagId)
```

虽然当前场景不会引发 bug（`res.data` 是一次性数据），但直接改变 DB 结果对象是坏的实践，阅读时难以追踪数据流。

**改进方案**

```js
const result = {
  ...detailData,
  level1_tags: (detailData.level1_tags || []).map(id => tagMap[id] || id).filter(Boolean),
  level2_tags: (detailData.level2_tags || []).map(id => tagMap[id] || id).filter(Boolean)
}
return { success: true, data: result }
```

---

## 三、低优先级 / 架构优化（P3）

### 3.1 `verifyAdmin` 逻辑在 4 个云函数中完全重复，每次调用均有额外 DB 查询

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `adminContent`、`adminTag`、`adminStore`、`adminUser` 各自 line 6-9 |
| **严重程度** | ★★★☆☆ 中 |

**问题描述**

4 个管理云函数各自声明了一份 `verifyAdmin`，每次调用额外消耗一次 `mixia_user` 查询。逻辑重复、维护成本高。

```js
// 4个文件中完全相同的代码
async function verifyAdmin(openid) {
  const res = await db.collection('mixia_user').where({ _openid: openid }).get()
  return res.data[0]?.isAdmin === true
}
```

**改进方案**

将 4 个管理云函数合并为单一 `admin` 云函数，按 `module` + `action` 路由，`verifyAdmin` 只声明一次：

```
cloudfunctions/
  admin/
    index.js    ← 统一入口，一次 verifyAdmin
    content.js  ← adminContent 逻辑
    tag.js      ← adminTag 逻辑
    store.js    ← adminStore 逻辑
    user.js     ← adminUser 逻辑
```

入口：
```js
exports.main = async (event, context) => {
  const { OPENID } = cloud.getWXContext()
  if (!(await verifyAdmin(OPENID))) return { success: false, error: '无权限' }

  const { module } = event
  const handlers = { content, tag, store, user }
  return handlers[module]?.(event, db) ?? { success: false, error: '未知模块' }
}
```

优点：
- 减少 3 次重复的 `verifyAdmin` DB 查询
- 从 4 个云函数冷启动变为 1 个
- 共享 `db`、`escapeRegex` 等工具

---

### 3.2 `compress.js` 并行上传时 `Date.now()` 可能产生相同时间戳，路径碰撞

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `miniprogram/utils/compress.js:115` |
| **严重程度** | ★★☆☆☆ 低 |

**问题描述**

```js
chooseRes.tempFiles.map(file => {
  const cloudPath = 'images/' + Date.now() + '-' + Math.random().toString(36).substr(2) + '.jpg';
  // 同一毫秒内多张图片 Date.now() 相同，random 碰撞概率虽低但存在
```

**改进方案**

加入文件索引确保唯一性：

```js
chooseRes.tempFiles.map((file, index) => {
  const cloudPath = `images/${Date.now()}-${index}-${Math.random().toString(36).substr(2)}.jpg`;
```

---

### 3.3 `list/index.wxml` 瀑布流左右列卡片 HTML 完全重复

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `miniprogram/pages/list/index.wxml:31-79` |
| **严重程度** | ★★☆☆☆ 低 |

**问题描述**

左列（`leftList`）和右列（`rightList`）的卡片模板代码完全相同，共约 24 行重复。任何样式或逻辑改动都需要修改两处。

**改进方案**

提取为自定义组件 `components/waterfall-card`，在 WXML 中复用：

```xml
<view class="waterfall-column">
  <block wx:for="{{leftList}}" wx:key="_id">
    <waterfall-card item="{{item}}" config="{{config}}" bindtap="goToDetail"></waterfall-card>
  </block>
</view>
```

---

### 3.4 `api.js` 对象字面量末尾语法冗余（`};` 前无逗号，但风格不统一）

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `miniprogram/utils/api.js:85-87` |
| **严重程度** | ★☆☆☆☆ 低 |

**问题描述**

```js
  adminUser(action, params = {}) {
    return this.callCloudFunction('adminUser', { action, ...params });
  }

  };  // ← 直接关闭对象，前面 adminStore 方法后没有逗号
```

不会导致运行时错误，但 `};` 混入对象字面量看起来像块级语句，可读性差，也会触发 linter 警告。

**改进方案**

给最后一个方法加尾逗号（与上面其他方法保持一致）：

```js
  adminUser(action, params = {}) {
    return this.callCloudFunction('adminUser', { action, ...params });
  },
};
```

---

### 3.5 `contact/index.js` 加载时无 `loading` 态，内容区域会出现空白闪烁

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `miniprogram/pages/contact/index.js:22-23` |
| **严重程度** | ★★☆☆☆ 低 |

**问题描述**

```js
loadStoreInfo: async function () {
  wx.showLoading({ title: '加载中' });
```

使用的是全局 `wx.showLoading`（遮罩模态），而非页面内骨架屏/占位。用户进入联系页时整个屏幕被白色遮罩覆盖，体验不佳。`detail/index.js` 已改用 `loading` 状态控制页面内 UI，`contact` 页面可做同样改造。

---

### 3.6 `admin/index.js` 在 `onShow` 重复执行 `checkAdmin`，每次返回都触发缓存读取

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `miniprogram/pages/admin/index.js:14-18` |
| **严重程度** | ★☆☆☆☆ 低 |

**问题描述**

```js
onLoad: function () { checkAdmin(); },
onShow: function () { checkAdmin(); },  // 每次从子页面返回都触发
```

对合法管理员无害（只是读缓存），但若 `userInfo` 缓存恰好在子页面操作后过期，会把管理员踢回首页。建议只在 `onLoad` 检查，或在 `onShow` 仅做静默刷新而不导航。

---

### 3.7 `list/index.js` - `loadData` 接收 `type` 参数但部分状态仍从 `this.data` 读取，语义不一致

| 项目 | 内容 |
|------|------|
| **缺陷位置** | `miniprogram/pages/list/index.js:71-99` |
| **严重程度** | ★☆☆☆☆ 低 |

**问题描述**

`loadData(type)` 接受 `type` 参数传给 `API.getModuleList`，但 `page`、`pageSize`、`currentTag`、`config` 全部从 `this.data` 读取。函数签名暗示 `type` 是驱动参数，但实际上所有状态都在 `this.data` 中，`type` 参数是多余的。

**改进方案**

移除参数，统一从 `this.data` 读取：

```js
loadData: async function () {
  const { type, page, pageSize, currentTag, config } = this.data;
  ...
}
// 所有调用处 loadData(type) → loadData()
```

---

## 四、优先级汇总

| 优先级 | 缺陷 | 位置 | 状态 |
|--------|------|------|------|
| **P1** | `onPullDownRefresh` 调用 `loadData().then()` 可能崩溃 | `list/index.js:46` | 待处理 |
| **P1** | 列表加载失败无用户反馈（api.js toast 已移除） | `list/index.js:95`、`admin/manage/index.js:80` | 待处理 |
| **P1** | `adminTag` 列表无 `.limit()`，超 20 条截断 | `adminTag/index.js:25` | 待处理 |
| **P1** | 并行上传用 `Promise.all`，单张失败导致孤立云存储文件 | `utils/compress.js:116` | 待处理 |
| **P1** | `admin/store` `onSubmit` `hideLoading` 不在 finally | `admin/store/index.js:171` | 待处理 |
| **P2** | `count()` + `get()` 串行，可改为 `Promise.all` | `getModuleList/index.js`、`adminContent/index.js` | 待处理 |
| **P2** | `getHomeData` 返回全量字段，缺 `.field()` 限制 | `getHomeData/index.js` | 待处理 |
| **P2** | 列表缓存 key 未对 tags 排序，顺序不同导致缓存穿透 | `utils/api.js:54` | 待处理 |
| **P2** | `CacheManager` 存取无错误捕获，存储异常向上崩溃 | `utils/cache.js` | 待处理 |
| **P2** | `adminContent save` 无字段白名单，可写入任意字段 | `adminContent/index.js:92` | 待处理 |
| **P2** | `adminTag toggleStatus` 后全量重新加载 | `admin/tags/index.js:55` | 待处理 |
| **P2** | `getModuleDetail` 直接修改 DB 返回对象 | `getModuleDetail/index.js:51` | 待处理 |
| **P3** | `verifyAdmin` 在 4 个云函数中重复，可合并为单一 admin 云函数 | `adminContent/Tag/Store/User` | 架构优化 |
| **P3** | 并行上传 cloudPath 含 `Date.now()`，毫秒内可能碰撞 | `utils/compress.js:115` | 待处理 |
| **P3** | 瀑布流左右列 WXML 模板重复 ~24 行 | `list/index.wxml:31-79` | 待处理 |
| **P3** | `api.js` 对象末尾 `};` 无前导逗号，风格不一致 | `utils/api.js:85` | 待处理 |
| **P3** | `contact/index.js` 用全局 `showLoading` 而非页面内 loading 态 | `contact/index.js:23` | 待处理 |
| **P3** | `admin/index.js` `onShow` 重复执行 checkAdmin | `admin/index.js:17` | 待处理 |
| **P3** | `loadData` 接收 `type` 参数但从 `this.data` 读取状态，语义冗余 | `list/index.js:71` | 待处理 |
| **待设计** | TabBar 图标无选中态 | `app.json` | 等待设计资源 |
